export default class CryptoUtils {



    /**
     * AES-256-CBC string encryption
     * @param key passhrase string used to encrypt the data
     * @param plain plain string string to encrypt
     * @returns 
     */
    public static encryptAes(key: string, plain: string): string {
        return '';
    }


    /**
     * AES-256-CBC string decryption
     * @param key passhrase string used to decrypt the data
     * @param encrypted encrypted string to decrypt
     * @returns 
     */
    public static decryptAes(key: string, encrypted: string): string {
        return ''

    }
}