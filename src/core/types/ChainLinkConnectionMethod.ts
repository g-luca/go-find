export class ChainLinkConnectionMethod {
    public id: string;
    public name: string;
    public logo: string;

    constructor(id: string, name: string, logo: string) {
        this.id = id;
        this.name = name;
        this.logo = logo;
    }
}